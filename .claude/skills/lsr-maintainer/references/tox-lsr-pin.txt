# Pinned tox-lsr install reference.
#
# This file declares the canonical install line for tox-lsr that the
# bootstrap-runner uses to create the venv at <paths.tox_venv> (default <workspace>/var/venv/tox-lsr/).
#
# Format: a single pip-compatible install spec on one non-comment line.
#
# Why pinned: tox-lsr can break between releases when a new ansible-core or
# new QEMU image format ships. Pinning lets us upgrade deliberately.
#
# NOTE: tox-lsr is NOT published on PyPI — it installs from the upstream
# GitHub repo. The commit hash below was the version validated on the
# operator's working venv at ~/github/ansible/testing/tox-lsr-venv/ (3.14.0,
# main branch). Bump by editing the commit hash to the latest tested SHA.
# To find the latest:
#   git ls-remote https://github.com/linux-system-roles/tox-lsr refs/heads/main

git+https://github.com/linux-system-roles/tox-lsr@565e2063bcbdac59e0e6b198177489577d6acd57
